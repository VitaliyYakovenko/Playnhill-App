

export default interface ISettings {
      dots?: boolean ,
      infinite: boolean,
      speed: number,
      slidesToShow: number,
      slidesToScroll: number,
      cssEase?: string,
      autoplay: boolean,
      autoplaySpeed: number,  
};